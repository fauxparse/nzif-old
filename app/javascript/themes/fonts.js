export default {
  base: 'Work Sans',
  branding: 'Brother',
  scale: step => `${Math.pow(1.2, step)}em`,
  lineHeight: 1.5,
  weights: {
    light: 300,
    normal: 400,
    bold: 600,
    black: 700,
  },
}
