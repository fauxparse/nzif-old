export default {
  primary: ({ theme }) => `
    background-color: ${theme.colors.accent};
    border: none;
    color: white;
    padding: 0.5em;
  `,

  icon: ({ theme }) => `
    border-radius: 50%;
  `
}
